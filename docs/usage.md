# AGI System Usage

## Prerequisites
- NASM (Netwide Assembler)
- Linux environment with x86_64 architecture

## Building the System
Run the following command to assemble and link the system:
```bash
bash tools/build.sh